[models.detect-fast]
path = "models/yolo26n.onnx"
task = "detect"
confidence = 0.5
imgsz = 640
# Optional: static ROI — only look at part of the frame
# [models.detect-fast.crop]
# type = "static"
# region = [100, 50, 500, 400]

[models.detect-large]
path = "models/yolo26x.onnx"
task = "detect"
confidence = 0.3
imgsz = 640

[models.detect-v2]
path = "models/yolo26s.onnx"
task = "detect"
confidence = 0.3
imgsz = 640

[models.pose-standard]
path = "models/yolo26n-pose.onnx"
task = "pose"
confidence = 0.3
imgsz = 640
# Optional: dynamic ROI — crop to largest "person" bbox from parent model
# [models.pose-standard.crop]
# type = "largest_class"
# class = "person"
# margin = 0.15

# --- disabled for now ---
# [models.face-v11]
# path = "models/yolov11n-face.onnx"
# task = "detect"
# confidence = 0.6
# imgsz = 640

# [models.face-v12]
# path = "models/yolov12n-face.onnx"
# task = "detect"
# confidence = 0.5
# imgsz = 640

# [models.segment-small]
# path = "models/yolo26n-seg.onnx"
# task = "segment"
# confidence = 0.3
# imgsz = 640

# [models.depth-small]
# path = "models/yolo26n-depth.onnx"
# task = "depth"
# imgsz = 768
