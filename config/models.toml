# Public model catalog entry point.
# The application keeps referencing this file; task-specific definitions live
# below config/models/ and are composed in the listed order.
include = [
    "models/base.toml",
    "models/detect.toml",
    "models/pose.toml",
    "models/segment.toml",
    "models/depth.toml",
]
