# Example public catalog entry point. In production, config/models.toml includes
# the complete task catalog. This smaller manifest demonstrates the same layers.
include = [
    "models/base.toml",
    "models/example-detect.toml",
]
