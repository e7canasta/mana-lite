/target/
/Cargo.lock

# Models (large binary files)
*.onnx
*.pt
*.pth
*.safetensors

.obsidian

/docs/.obsidian

# Output
# logs/
*.jsonl
# ...but the golden reference is a fixture, not output: without it committed
# `git diff tests/golden/` passes vacuously and no fresh clone can run the test.
!tests/golden/*.jsonl

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store
snapshots/

# Local agent state and worktrees
/.kiro/
/.claude/settings.local.json
/.claude/worktrees/*/
workshop/runs/

# Local replay and calibration output. Curated JSON fixtures live under
# tests/fixtures/ and source calibration/configuration remains versioned.
/runs/
/demo-deep-calib/
/demo-deep-calib-radio/
/demo-deep-calib-parts/
/scripts/frames/
/logs/
/dist/
/config/workshop/deep-calib-depth-*.toml
!/config/workshop/deep-calib-depth-l-640.toml
/*.jpeg
/*.jpg
/*.png
*.calib-*.png
*.calib-pose*.png
*.calib-preview*.png
*.log
*.tmp.*
