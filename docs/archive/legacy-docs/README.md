# Documentacion legacy archivada

Las guias antiguas de `docs/`, la wiki generada y el workshop duplicado fueron
reemplazados por la documentacion activa. El contenido se conserva en el
historial de Git; este directorio solo marca la frontera de archivado.

Referencias actuales:

- `ARCHITECTURE.md` en la raiz;
- `docs/README.md`;
- `docs/adrs/`;
- `docs/specs/`;
- `workshop/`.
