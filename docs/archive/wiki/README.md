# Wiki historica

La wiki fue una referencia generada contra una version anterior del runtime. Se
conserva solo la edicion `0.1` como archivo historico. La referencia vigente es
`ARCHITECTURE.md`, junto con `docs/specs/`, `docs/adrs/`, `workshop/` y
`docs/subprojects/`.
