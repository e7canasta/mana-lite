[blueprint]
name = "inference-capacity-m-640"
description = "Capacity benchmark for detect, face, pose and segmentation at m/640"
primary_model = "detect-fast"
models = ["detect-fast", "face-yolo", "pose-standard", "seg-standard"]
requires_tracking = true
model_overlay = "models-m-640.toml"

[[rules]]
model = "detect-fast"
interval_min_ms = 0

[[rules]]
model = "face-yolo"
requires = "detect-fast"
requires_class = "person"
requires_exact_count = 1
requires_min_confidence = 0.50
interval_min_ms = 0

[[rules]]
model = "pose-standard"
requires = "detect-fast"
requires_class = "person"
requires_exact_count = 1
requires_min_confidence = 0.50
interval_min_ms = 0

[[rules]]
model = "seg-standard"
requires = "detect-fast"
requires_class = "person"
requires_exact_count = 1
requires_min_confidence = 0.50
interval_min_ms = 2000
